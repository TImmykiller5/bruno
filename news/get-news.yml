info:
  name: get-news
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/news?page=1&limit=100"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "100"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
