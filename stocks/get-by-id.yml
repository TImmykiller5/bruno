info:
  name: get-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/stocks/:symbol"
  params:
    - name: symbol
      value: ACCESSCORP
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
