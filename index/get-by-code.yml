info:
  name: get-by-code
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/index/:symbol"
  params:
    - name: symbol
      value: FIRSTHOLDCO
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
