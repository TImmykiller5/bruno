info:
  name: preview
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/market/preview"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
