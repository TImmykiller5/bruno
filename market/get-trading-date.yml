info:
  name: get-trading-date
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/market/trading-day"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
