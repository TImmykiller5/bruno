info:
  name: get-all Copy
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/sectors/sectors-top-3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
